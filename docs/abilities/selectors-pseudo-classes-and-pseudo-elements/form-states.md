# Form states

<FeatureLinks mdn="https://developer.mozilla.org/en-US/search?q=%3Aplaceholder-shown%20CSS" caniuse="https://caniuse.com/?search=%3Aplaceholder-shown" status="Selectors 4/5" label="Reference links" />

## Plain-language explanation

Respond to validity, defaults, required fields, and user interaction from CSS.

## Representative syntax

```css
:placeholder-shown, :default, :valid, :invalid, :in-range, :out-of-range, :required, :optional, :user-valid, :user-invalid
```

## Demo

<CssDemo title="Form states" code=":placeholder-shown, :default, :valid, :invalid, :in-range, :out-of-range, :required, :optional, :user-valid, :user-invalid" note="Support for draft capabilities varies across browsers. This demo focuses on syntax and progressive enhancement; use the Can I Use link for the current compatibility matrix." lang="en-US" badge="Live-ish" />

## When to use it

- Consider it when you need to solve problems like: Respond to validity, defaults, required fields, and user interaction from CSS.
- If the status includes draft, limited support, or implementation in progress, use @supports, feature detection, or conservative fallbacks.
- For older target browsers, verify support on MDN and Can I Use before using it in production.

## Source metadata

- Category: Selectors, Pseudo-classes, and Pseudo-elements
- Maturity / source: Selectors 4/5
- Representative syntax: `:placeholder-shown, :default, :valid, :invalid, :in-range, :out-of-range, :required, :optional, :user-valid, :user-invalid`
- Position in category: 6 / 15
