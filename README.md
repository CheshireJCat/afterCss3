# After CSS3

CSS3 之后的新 CSS 能力导航。项目使用 VitePress 构建，可以部署到 GitHub Pages。

## 本地开发

```bash
npm install
npm run docs:dev
```

## 构建

```bash
npm run docs:build
npm run docs:preview
```

文档源在 `docs/`，能力数据在 `data/abilities.json`。修改数据后运行：

```bash
npm run docs:generate
```
